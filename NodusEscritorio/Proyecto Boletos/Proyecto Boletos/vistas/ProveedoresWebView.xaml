<Window x:Class="Proyecto_Boletos.vistas.ProveedoresWebView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Proveedores Web" Height="550" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Color3}"
        Icon="/Logo_Nodus.png">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TÍTULO -->
        <TextBlock Text="Proveedores desde la Web"
               FontSize="24"
               FontWeight="Bold"
               Foreground="{DynamicResource Color1}"
               Margin="0,0,0,20"/>

        <!-- DATAGRID -->
        <Border Grid.Row="1"
            BorderBrush="{DynamicResource Color1}"
            BorderThickness="1"
            CornerRadius="5">

            <DataGrid x:Name="dgProveedoresWeb"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  BorderThickness="0"
                  RowBackground="White"
                  AlternatingRowBackground="#97BBA9"
                  RowHeight="45"
                  ColumnHeaderHeight="45"
                  FontSize="14"
                  VerticalScrollBarVisibility="Hidden">

                <!-- HEADER STYLE -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource Color1}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <!-- ROW STYLE -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Style>
                </DataGrid.RowStyle>

                <!-- CELL STYLE -->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Color1}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <!-- COLUMNAS -->
                <DataGrid.Columns>

                    <DataGridTextColumn Header="NIT"
                                    Binding="{Binding NitProveedor}"
                                    Width="120"
                                    ElementStyle="{StaticResource TextoCentrado}"/>

                    <DataGridTextColumn Header="Nombre Comercial"
                                    Binding="{Binding NombreComercial}"
                                    Width="*"
                                    ElementStyle="{StaticResource TextoCentrado}"/>

                    <DataGridTextColumn Header="Teléfono"
                                    Binding="{Binding TelefonoComercial}"
                                    Width="150"
                                    ElementStyle="{StaticResource TextoCentrado}"/>

                    <DataGridTextColumn Header="Ciudad"
                                    Binding="{Binding IdCiudad}"
                                    Width="80"
                                    ElementStyle="{StaticResource TextoCentrado}"/>

                    <DataGridTextColumn Header="Estado"
                                    Binding="{Binding EstadoProveedor}"
                                    Width="120"
                                    ElementStyle="{StaticResource TextoCentrado}"/>

                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- BOTÓN -->
        <Button Grid.Row="2"
            Content="Cerrar"
            HorizontalAlignment="Right"
            Width="100"
            Height="35"
            Margin="0,15,0,0"
            Background="{DynamicResource Color1}"
            Foreground="White"
            BorderBrush="{DynamicResource Color1}"
            Cursor="Hand"
            Click="btnCerrar_Click">

            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>