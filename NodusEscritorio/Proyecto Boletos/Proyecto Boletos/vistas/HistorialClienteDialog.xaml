<Window
  x:Class="Proyecto_Boletos.vistas.HistorialClienteDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="Historial del Cliente"
  Height="520" Width="780"
  WindowStartupLocation="CenterOwner"
  ResizeMode="CanResizeWithGrip"
  Icon="/Logo_Nodus.png"
  Background="{DynamicResource Color3}"
  Loaded="Window_Loaded"
>
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Encabezado cliente -->
    <Border Background="{DynamicResource Color1}" CornerRadius="8" Padding="16,12" Margin="0,0,0,16">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock x:Name="txtNombreCliente" FontSize="18" FontWeight="Bold" Foreground="White" />
          <TextBlock x:Name="txtInfoCliente" FontSize="12" Foreground="#CCFFFFFF" Margin="0,2,0,0" />
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="20,0" HorizontalAlignment="Right">
          <TextBlock Text="Eventos" FontSize="11" Foreground="#AAFFFFFF" />
          <TextBlock x:Name="txtTotalEventos" Text="0" FontSize="22" FontWeight="Bold" Foreground="White" />
        </StackPanel>
        <StackPanel Grid.Column="2" Margin="20,0" HorizontalAlignment="Right">
          <TextBlock Text="Ordenes" FontSize="11" Foreground="#AAFFFFFF" />
          <TextBlock x:Name="txtTotalOrdenes" Text="0" FontSize="22" FontWeight="Bold" Foreground="White" />
        </StackPanel>
        <StackPanel Grid.Column="3" Margin="20,0,0,0" HorizontalAlignment="Right">
          <TextBlock Text="Total gastado" FontSize="11" Foreground="#AAFFFFFF" />
          <TextBlock x:Name="txtTotalGastado" Text="Bs 0" FontSize="22" FontWeight="Bold" Foreground="White" />
        </StackPanel>
      </Grid>
    </Border>

    <!-- Tabs: Eventos | Ordenes -->
    <TabControl Grid.Row="1" Grid.RowSpan="2">
      <TabItem Header="  Eventos del Cliente  ">
        <Border Background="White" BorderBrush="{DynamicResource Color1}" BorderThickness="0,1,0,0">
          <DataGrid x:Name="dgEventos" AutoGenerateColumns="False" IsReadOnly="False"
                    HeadersVisibility="Column" GridLinesVisibility="None"
                    Background="Transparent" BorderThickness="0"
                    CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Evento" Binding="{Binding NombreEvento}" Width="*" IsReadOnly="True" />
              <DataGridTextColumn Header="Recinto" Binding="{Binding NombreRecinto}" Width="150" IsReadOnly="True" />
              <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="110" IsReadOnly="True" />
              <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100" IsReadOnly="True" />
              <DataGridTemplateColumn Header="" Width="90">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Content="Editar" Tag="{Binding}"
                            Height="26" Padding="8,0" FontSize="11"
                            Background="{DynamicResource Color1}" Foreground="White"
                            BorderBrush="{DynamicResource Color1}" Cursor="Hand"
                            Click="btnEditarEvento_Click" />
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
            </DataGrid.Columns>
          </DataGrid>
        </Border>
      </TabItem>

      <TabItem Header="  Ordenes / Pagos  ">
        <Border Background="White" BorderBrush="{DynamicResource Color1}" BorderThickness="0,1,0,0">
          <DataGrid x:Name="dgOrdenes" AutoGenerateColumns="False" IsReadOnly="True"
                    HeadersVisibility="Column" GridLinesVisibility="None"
                    Background="Transparent" BorderThickness="0">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="130" />
              <DataGridTextColumn Header="Comprador" Binding="{Binding Comprador}" Width="*" />
              <DataGridTextColumn Header="NIT" Binding="{Binding Nit}" Width="100" />
              <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="90" />
              <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="90" />
            </DataGrid.Columns>
          </DataGrid>
        </Border>
      </TabItem>
    </TabControl>
  </Grid>
</Window>
