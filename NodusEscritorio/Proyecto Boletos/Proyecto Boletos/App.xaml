<Application
  x:Class="Proyecto_Boletos.App"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="clr-namespace:Proyecto_Boletos"
  StartupUri="MainWindow.xaml"
  xmlns:sys="clr-namespace:System;assembly=mscorlib"
>
  <Application.Resources>
    <!-- Modo claro -->
    <Color x:Key="Claro1">#096C4B</Color>
    <Color x:Key="Claro2">#97BBA9</Color>
    <Color x:Key="Claro3">#D4A373</Color>
    <Color x:Key="Claro4">#000</Color>

    <!-- Modo oscuro -->
    <Color x:Key="Oscuro1">#095B6C</Color>
    <Color x:Key="Oscuro2">#0F112E</Color>
    <Color x:Key="Oscuro3">#2D3142</Color>
    <Color x:Key="Oscuro4">#fff</Color>

    <!-- Brushes dinámicos -->
    <SolidColorBrush x:Key="Color1" Color="{DynamicResource Claro1}" />
    <SolidColorBrush x:Key="Color2" Color="{DynamicResource Claro2}" />
    <SolidColorBrush x:Key="Color3" Color="{DynamicResource Claro3}" />
    <SolidColorBrush x:Key="Color4" Color="{DynamicResource Claro4}" />

    <!-- Estilos -->
    <Style x:Key="InputStyle" TargetType="TextBox">
      <Setter Property="Margin" Value="0,10" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Background" Value="White" />
      <Setter Property="BorderBrush" Value="{DynamicResource Color3}" />
      <Setter Property="BorderThickness" Value="1.5" />
    </Style>

    <Style x:Key="PasswordStyle" TargetType="PasswordBox">
      <Setter Property="Margin" Value="0,10" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Background" Value="White" />
      <Setter Property="BorderBrush" Value="{DynamicResource Color3}" />
      <Setter Property="BorderThickness" Value="1.5" />
    </Style>

    <Style x:Key="ButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource Color1}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="Margin" Value="0,20,0,0" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="SidebarButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Padding" Value="15,10" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="InfoUsuario" TargetType="TextBlock">
      <Setter Property="VerticalAlignment" Value="center" />
      <Setter Property="Margin" Value="10 0" />
      <Setter Property="FontSize" Value="20" />
      <Setter Property="Foreground" Value="White" />
    </Style>

    <!-- Imagenes -->
    <BitmapImage x:Key="imgClaro" UriSource="/estadio_dia.jpeg" />
    <BitmapImage x:Key="imgOscuro" UriSource="/estadio_noche.jpeg" />
    <BitmapImage x:Key="imgActual" UriSource="/estadio_dia.jpeg" />

    <!-- Iconos -->
    <sys:String x:Key="txtClaro">&#xE706;</sys:String>
    <sys:String x:Key="txtOscuro">&#xE708;</sys:String>
    <sys:String x:Key="txtActual">&#xE706;</sys:String>

    <!-- Texto -->
    <LinearGradientBrush x:Key="TituloClaro" StartPoint="0,0" EndPoint="1,0">
      <GradientStop Color="White" Offset="0" />
      <GradientStop Color="#97BBA9" Offset="0.5" />
      <GradientStop Color="#096C4B" Offset="1" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="TituloOscuro" Color="White" />

    <LinearGradientBrush x:Key="TituloActual" StartPoint="0,0" EndPoint="1,0">
      <GradientStop Color="White" Offset="0" />
      <GradientStop Color="#97BBA9" Offset="0.5" />
      <GradientStop Color="#096C4B" Offset="1" />
    </LinearGradientBrush>

    <!-- DataGrid -->
    <Style x:Key="TextoCentrado" TargetType="TextBlock">
      <Setter Property="TextAlignment" Value="Center" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

    <!-- HEADER STYLE -->
    <Style x:Key="ModernDataGridHeaderStyle" TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="{DynamicResource Color1}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>

    <!-- ROW STYLE -->
    <Style x:Key="ModernDataGridRowStyle" TargetType="DataGridRow">
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Cursor" Value="Hand" />

      <Style.Triggers>
        <!-- Hover -->
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#97FBA9" />
        </Trigger>
      </Style.Triggers>
    </Style>

    <!-- CELL STYLE -->
    <Style x:Key="ModernDataGridCellStyle" TargetType="DataGridCell">
      <Setter Property="Padding" Value="10,5" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="TextBlock.TextAlignment" Value="Center" />
      <Setter Property="VerticalContentAlignment" Value="Center" />

      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{DynamicResource Color3}" />
          <Setter Property="Foreground" Value="{DynamicResource Color4}" />
        </Trigger>
      </Style.Triggers>
    </Style>

    <!-- ESTILO GLOBAL DATAGRID -->
    <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
      <Setter Property="AutoGenerateColumns" Value="False" />
      <Setter Property="IsReadOnly" Value="True" />
      <Setter Property="CanUserAddRows" Value="False" />
      <Setter Property="HeadersVisibility" Value="Column" />
      <Setter Property="GridLinesVisibility" Value="None" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="RowBackground" Value="White" />
      <Setter Property="AlternatingRowBackground" Value="#97BBA9" />
      <Setter Property="RowHeight" Value="45" />
      <Setter Property="ColumnHeaderHeight" Value="45" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="VerticalScrollBarVisibility" Value="Hidden" />

      <Setter Property="ColumnHeaderStyle" Value="{StaticResource ModernDataGridHeaderStyle}" />

      <Setter Property="RowStyle" Value="{StaticResource ModernDataGridRowStyle}" />

      <Setter Property="CellStyle" Value="{StaticResource ModernDataGridCellStyle}" />
    </Style>
  </Application.Resources>
</Application>
